# `ops_tables`

This unit covers the operational target tables populated by
`databricks/load/02_backfill_core.sql` and the target schema setup in
`databricks/ddl/01_schemas.sql` and `databricks/ddl/02_core_tables.sql`.
Its authored mapping is `units/ops_tables/mapping.json`.
